# Database Configuration
# Use ONE of these database URLs:

# For development with Neon (current setup)
DATABASE_URL=postgresql://user:password@host/database

# For production with Supabase
SUPABASE_DATABASE_URL=postgresql://postgres.project-ref:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Firebase Configuration - Client (Frontend)
# Get these from Firebase Console > Project Settings > General
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_APP_ID=your-firebase-app-id

# Firebase Admin - Server (Backend)
# Option 1: Individual credentials
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Option 2: Service Account JSON (as base64 or JSON string)
# Download from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"..."}

# Session Management
SESSION_SECRET=your-super-secret-session-key-here
# Alternative session secret (Vercel/NextAuth compatible)
NEXTAUTH_SECRET=your-nextauth-secret-here

# Optional: Global Binance API Keys (for fallback/testing)
# Note: Users configure their own API keys in the app settings
BINANCE_API_KEY=your_optional_binance_api_key
BINANCE_SECRET_KEY=your_optional_binance_secret_key

# Email Service (optional)
# SMTP Configuration (Brevo, Gmail, SendGrid, etc.)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your-smtp-user@smtp-provider.com
SMTP_PASS=your-smtp-password

# Sender email configuration (must be validated in Brevo)
# If not specified, SMTP_USER will be used as sender
SMTP_FROM_EMAIL=your-validated-email@example.com
SMTP_FROM_NAME=Binance Trading Dashboard

# Alternative: SendGrid API (if not using SMTP)
SENDGRID_API_KEY=your_sendgrid_api_key

# Environment
NODE_ENV=development